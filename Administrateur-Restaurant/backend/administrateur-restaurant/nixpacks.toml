[build]
plan = "php"

[phases.setup]
aptPkgs = ["php-mysql", "php-gd", "php-xml", "php-mbstring"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.start]
cmd = "php artisan migrate --force && php artisan serve --host 0.0.0.0 --port $PORT"
